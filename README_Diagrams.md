
┌─────────────────────────────────────────────────────────────────┐
│                         Twitter APP                                 │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│   👤 Users can:                                                  │
│      • Register & Login                                          │
│      • Post tweets                                               │
│      • Like & Retweet                                           │
│      • Follow other users                                        │
│      • Search tweets & users                                     │
│      • View timeline                                             │
│                                                                  │
│   🔧 Behind the scenes:                                          │
│      • Data stored in PostgreSQL (database)                     │
│      • Fast reads via Redis (cache)                             │
│      • Events processed via Kafka (message queue)               │
│      • Search powered by Elasticsearch                          │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘

find . -type f -name "*.js" -o -name "*.jsx" -o -name "*.json" -o -name "*.yml" -o -name "*.sql" -o -name "Dockerfile" 2>/dev/null | grep -v node_modules | sort

twitter/
│
├── docker-compose.yml      ← 🐳 Defines ALL services (database, cache, etc.)
│
├── database/               ← 📊 SQL scripts to set up database
│   ├── init-primary.sql    ← Creates tables (users, tweets, likes, etc.)
│   └── partitioning.sql    ← Splits tweets table by date
│
├── backend/                ← 🖥️ Server-side code (Node.js + Express)
│   ├── Dockerfile          ← Instructions to build backend container
│   ├── package.json        ← Lists all dependencies (libraries)
│   └── src/
│       ├── server.js       ← Main entry point - starts the server
│       ├── database/       ← Database connection
│       ├── middleware/     ← Functions that run before routes
│       ├── routes/         ← API endpoints (/api/tweets, /api/users, etc.)
│       └── services/       ← External services (Redis, Kafka, Elasticsearch)
│
└── frontend/               ← 🎨 Client-side code (React)
    ├── Dockerfile          ← Instructions to build frontend container
    ├── package.json        ← Lists all dependencies
    └── src/
        ├── App.jsx         ← Main React component with routes
        ├── pages/          ← Full page components (Home, Profile, etc.)
        ├── components/     ← Reusable UI pieces (TweetCard, Sidebar, etc.)
        ├── api/            ← Functions to call backend APIs
        └── stores/         ← Global state management

WITHOUT DOCKER:
┌─────────────────────────────────────────┐
│  Your Computer                          │
│  ┌─────────────────────────────────────┐│
│  │ "It works on my machine!" 🤷        ││
│  │                                      ││
│  │ • Different Node version            ││
│  │ • Different PostgreSQL version      ││
│  │ • Missing dependencies              ││
│  │ • Different OS settings             ││
│  └─────────────────────────────────────┘│
└─────────────────────────────────────────┘

WITH DOCKER:
┌─────────────────────────────────────────┐
│  Your Computer                          │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐│
│  │Container │ │Container │ │Container ││
│  │ Node 18  │ │Postgres  │ │  Redis   ││
│  │ Express  │ │   15     │ │    7     ││
│  └──────────┘ └──────────┘ └──────────┘│
│                                         │
│  Every developer gets EXACT same setup! │
└─────────────────────────────────────────┘

Request comes in
        │
        ▼
┌───────────────┐
│   helmet()    │  ← Adds security headers
└───────┬───────┘
        │
        ▼
┌───────────────┐
│    cors()     │  ← Checks if frontend is allowed
└───────┬───────┘
        │
        ▼
┌───────────────┐
│   json()      │  ← Parses JSON body
└───────┬───────┘
        │
        ▼
┌───────────────┐
│ rateLimiter() │  ← Prevents too many requests
└───────┬───────┘
        │
        ▼
┌───────────────┐
│  Your Route   │  ← Actually handles the request
└───────┬───────┘
        │
        ▼
Response sent back

Frontend sends:
POST http://localhost:3001/api/tweets
{
  "content": "Hello World!"
}

                    │
                    ▼
┌──────────────────────────────────────────────────────────┐
│ 1. authenticate middleware                               │
│    - Checks JWT token in Authorization header            │
│    - If valid, adds req.user = { userId: 1 }            │
│    - If invalid, returns 401 Unauthorized               │
└──────────────────────────────────────────────────────────┘
                    │
                    ▼
┌──────────────────────────────────────────────────────────┐
│ 2. validation middleware                                 │
│    - Checks: is content 1-280 characters?               │
│    - If invalid, returns 400 Bad Request                │
└──────────────────────────────────────────────────────────┘
                    │
                    ▼
┌──────────────────────────────────────────────────────────┐
│ 3. route handler                                         │
│    - Insert tweet into PostgreSQL                        │
│    - Publish event to Kafka                              │
│    - Clear Redis cache                                   │
│    - Send WebSocket notification                         │
│    - Return 201 Created with tweet data                  │
└──────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                         DATABASE TABLES                          │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│ USERS TABLE                                                      │
├──────────────┬───────────────┬──────────────────────────────────┤
│ Column       │ Type          │ What it stores                   │
├──────────────┼───────────────┼──────────────────────────────────┤
│ id           │ BIGSERIAL     │ Unique ID (auto-generated)       │
│ username     │ VARCHAR(15)   │ @handle (unique)                 │
│ email        │ VARCHAR(254)  │ Email address (unique)           │
│ password_hash│ VARCHAR(255)  │ Encrypted password               │
│ display_name │ VARCHAR(50)   │ Shown name                       │
│ bio          │ TEXT          │ Profile description              │
│ follower_count│ INTEGER      │ How many follow this user        │
│ following_count│ INTEGER     │ How many this user follows       │
│ tweet_count  │ INTEGER       │ Total tweets                     │
│ created_at   │ TIMESTAMP     │ When account was created         │
└──────────────┴───────────────┴──────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│ TWEETS TABLE                                                     │
├──────────────┬───────────────┬──────────────────────────────────┤
│ id           │ BIGSERIAL     │ Unique tweet ID                  │
│ user_id      │ BIGINT        │ Who posted it (→ users.id)       │
│ content      │ TEXT          │ The tweet text (max 280 chars)   │
│ like_count   │ INTEGER       │ Number of likes                  │
│ retweet_count│ INTEGER       │ Number of retweets               │
│ created_at   │ TIMESTAMP     │ When it was posted               │
└──────────────┴───────────────┴──────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│ FOLLOWS TABLE (Many-to-Many Relationship)                        │
├──────────────┬───────────────┬──────────────────────────────────┤
│ follower_id  │ BIGINT        │ Who is following (→ users.id)    │
│ following_id │ BIGINT        │ Who is being followed (→ users.id)│
└──────────────┴───────────────┴──────────────────────────────────┘

Example: User 1 follows User 2
         follows table row: {follower_id: 1, following_id: 2}

Without index:                  With index:
┌─────────────────────────┐    ┌─────────────────────────┐
│ Find tweets by user 5   │    │ Find tweets by user 5   │
│                         │    │                         │
│ Check row 1... no       │    │ INDEX says: "User 5's   │
│ Check row 2... no       │    │ tweets are at rows      │
│ Check row 3... no       │    │ 847, 1023, 1456"        │
│ ... check ALL rows      │    │                         │
│ (slow! O(n))           │    │ Go directly there!      │
└─────────────────────────┘    │ (fast! O(log n))        │
                               └─────────────────────────┘

WITHOUT PARTITIONING:
┌─────────────────────────────────────────────────────────────────┐
│                    tweets (1 BILLION rows)                       │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │ All tweets from 2020-2026 in ONE HUGE table              │   │
│  │ Query: "Get today's tweets" → Must scan EVERYTHING       │   │
│  └──────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘

WITH PARTITIONING (by quarter):
┌─────────────────────────────────────────────────────────────────┐
│                    tweets (partitioned table)                    │
│                                                                  │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐        │
│  │ 2025_Q1  │  │ 2025_Q2  │  │ 2025_Q3  │  │ 2025_Q4  │        │
│  │ Jan-Mar  │  │ Apr-Jun  │  │ Jul-Sep  │  │ Oct-Dec  │        │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘        │
│                                                                  │
│  ┌──────────┐  ┌──────────┐                                     │
│  │ 2026_Q1  │  │ 2026_Q2  │  ← Query for Feb 2026 only         │
│  │ Jan-Mar  │  │ Apr-Jun  │    scans this ONE partition!       │
│  └──────────┘  └──────────┘                                     │
└─────────────────────────────────────────────────────────────────┘

┌────────────────────────────────────────────────────────────────┐
│                    CACHE-ASIDE PATTERN                          │
└────────────────────────────────────────────────────────────────┘

READING DATA:

User requests timeline
        │
        ▼
┌───────────────────┐
│  1. Check Redis   │───── Cache HIT? ─────► Return cached data
│     cache first   │         │              (super fast!)
└───────────────────┘         │
                              │ Cache MISS
                              ▼
┌───────────────────┐   ┌───────────────────┐
│ 2. Query PostgreSQL│──►│ 3. Store in Redis │
│    (slower)        │   │    for next time  │
└───────────────────┘   └───────────────────┘
                              │
                              ▼
                        Return data


Speed comparison:
┌─────────────────────────────────────────────────────────────┐
│ Redis (RAM)     : ~0.5 ms   ████                            │
│ PostgreSQL (Disk): ~5-50 ms ████████████████████████████████│
└─────────────────────────────────────────────────────────────┘
                    Redis is 10-100x faster!

WITHOUT KAFKA (Synchronous):
┌──────────┐                          ┌──────────┐
│  User    │───── Create Tweet ──────►│  Backend │
│  waits   │                          │          │
│   ...    │                          │ 1. Save to DB (50ms)
│   ...    │                          │ 2. Index in Elastic (100ms)
│   ...    │                          │ 3. Send notifications (200ms)
│   ...    │                          │ 4. Update analytics (50ms)
│   ...    │◄───── Response (400ms) ──│          │
└──────────┘                          └──────────┘
     Total wait time: 400ms 😴


WITH KAFKA (Asynchronous):
┌──────────┐                          ┌──────────┐
│  User    │───── Create Tweet ──────►│  Backend │
│          │                          │          │
│          │◄───── Response (50ms) ───│ 1. Save to DB
│          │                          │ 2. Publish to Kafka
└──────────┘                          └──────────┘
     User gets response in 50ms! 🚀        │
                                          │
                                          ▼
                                    ┌──────────┐
                                    │  KAFKA   │ (Message Queue)
                                    │  TOPIC   │
                                    └────┬─────┘
                    ┌────────────────────┼────────────────────┐
                    ▼                    ▼                    ▼
             ┌──────────┐         ┌──────────┐         ┌──────────┐
             │ Consumer │         │ Consumer │         │ Consumer │
             │ Elastic  │         │ Notify   │         │Analytics │
             │ Indexing │         │ Service  │         │ Service  │
             └──────────┘         └──────────┘         └──────────┘
             
             These run in the background, user doesn't wait!

┌─────────────────────────────────────────────────────────────────┐
│                            App                                   │
│    (Main component, handles routing)                            │
└───────────────────────────┬─────────────────────────────────────┘
                            │
            ┌───────────────┴───────────────┐
            ▼                               ▼
     ┌──────────┐                    ┌──────────┐
     │  Login   │                    │  Layout  │
     │  Page    │                    │(Wrapper) │
     └──────────┘                    └────┬─────┘
                                          │
                    ┌─────────────────────┼─────────────────────┐
                    ▼                     ▼                     ▼
             ┌──────────┐          ┌──────────┐          ┌──────────┐
             │ Sidebar  │          │   Home   │          │  Profile │
             │          │          │   Page   │          │   Page   │
             └──────────┘          └────┬─────┘          └──────────┘
                                        │
                            ┌───────────┴───────────┐
                            ▼                       ▼
                     ┌──────────┐            ┌──────────┐
                     │ Compose  │            │TweetCard │
                     │  Tweet   │            │(multiple)│
                     └──────────┘            └──────────┘


┌─────────────────────────────────────────────────────────────────────┐
│                    POSTING A TWEET - FULL FLOW                       │
└─────────────────────────────────────────────────────────────────────┘

1️⃣ USER ACTION
┌──────────────┐
│   Browser    │  User types "Hello World!" and clicks Tweet
│   (React)    │
└──────┬───────┘
       │
       │  POST http://localhost:3001/api/tweets
       │  { "content": "Hello World!" }
       │  Headers: { Authorization: "Bearer eyJhbG..." }
       │
       ▼
2️⃣ DOCKER NETWORK
┌──────────────────────────────────────────────────────────────────┐
│                    Docker Internal Network                        │
│  ┌────────────────────────────────────────────────────────────┐  │
│  │                                                            │  │
│  │    ┌──────────┐         3️⃣ BACKEND PROCESSING             │  │
│  │    │ Backend  │◄────────────────────────────────────────┐  │  │
│  │    │ Express  │                                         │  │  │
│  │    └────┬─────┘                                         │  │  │
│  │         │                                               │  │  │
│  │         │ a. Validate JWT token (auth middleware)       │  │  │
│  │         │ b. Validate content length (validation)       │  │  │
│  │         │ c. Begin database transaction                 │  │  │
│  │         │                                               │  │  │
│  │         ▼                                               │  │  │
│  │    ┌──────────┐                                         │  │  │
│  │    │PostgreSQL│  4️⃣ INSERT INTO tweets                  │  │  │
│  │    │ Primary  │  (user_id, content) VALUES (1, 'Hello') │  │  │
│  │    └────┬─────┘                                         │  │  │
│  │         │                                               │  │  │
│  │         │ Returns: { id: 123, content: "Hello World!" } │  │  │
│  │         ▼                                               │  │  │
│  │    ┌──────────┐                                         │  │  │
│  │    │  Kafka   │  5️⃣ PUBLISH EVENT                       │  │  │
│  │    │ Producer │  Topic: "tweets"                        │  │  │
│  │    └────┬─────┘  Event: { type: "tweet.created", ... }  │  │  │
│  │         │                                               │  │  │
│  │         ▼                                               │  │  │
│  │    ┌──────────┐                                         │  │  │
│  │    │  Redis   │  6️⃣ INVALIDATE CACHE                    │  │  │
│  │    │  Cache   │  DEL timeline:1:*                       │  │  │
│  │    └──────────┘  (so next request gets fresh data)      │  │  │
│  │                                                         │  │  │
│  └─────────────────────────────────────────────────────────┘  │
│                                                                │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ BACKGROUND PROCESSING (Async - user doesn't wait)        │  │
│  │                                                          │  │
│  │    ┌──────────┐         ┌──────────────┐                │  │
│  │    │  Kafka   │────────►│ Elasticsearch │  7️⃣ INDEX     │  │
│  │    │ Consumer │         │   Consumer    │  for search   │  │
│  │    └──────────┘         └──────────────┘                │  │
│  │                                                          │  │
│  └──────────────────────────────────────────────────────────┘  │
└────────────────────────────────────────────────────────────────┘

8️⃣ RESPONSE
┌──────────────┐
│   Browser    │◄──── 201 Created
│   (React)    │      { id: 123, content: "Hello World!", ... }
└──────────────┘
       │
       │  React updates UI with new tweet
       ▼
    User sees their tweet instantly! 🎉
