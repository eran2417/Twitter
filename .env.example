# Environment variables for development
# Copy this file to .env and update values

# Backend
cp backend/.env.example backend/.env

# Update JWT secret in backend/.env
# Update database passwords for production
# Update Kafka broker URLs for external access

# Frontend - create .env file
echo "VITE_API_URL=http://localhost:3001" > frontend/.env
echo "VITE_WS_URL=ws://localhost:3001" >> frontend/.env
